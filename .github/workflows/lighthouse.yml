name: Lighthouse CI

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      url:
        description: 'URL to run Lighthouse on'
        required: false
        default: 'http://localhost:8080'

permissions:
  issues: write
  contents: read

jobs:
  lighthouse-audit:
    name: Lighthouse Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10

      - name: Install dependencies
        run: |
          pnpm install --frozen-lockfile
          npm install -g @lhci/cli@0.14.x

      - name: Build project
        run: pnpm run build

      - name: Start local server
        run: |
          npx http-server dist -p 8080 -c-1 &
          echo "Waiting for server to start..."
          for i in {1..30}; do
            if curl -f http://localhost:8080 > /dev/null 2>&1; then
              echo "Server is ready!"
              break
            fi
            sleep 1
          done
          curl -f http://localhost:8080 || exit 1

      - name: Run Lighthouse CI
        id: lighthouse
        continue-on-error: true
        run: |
          echo "Running Lighthouse CI..."
          lhci autorun || {
            echo "Lighthouse CI failed, trying alternative method..."
            npx @lhci/cli@0.14.x autorun || echo "All Lighthouse methods failed"
          }
          echo "Lighthouse CI execution completed"

      - name: Check Lighthouse reports
        if: always()
        run: |
          if [ -d ".lighthouseci" ]; then
            echo "Lighthouse reports directory exists"
            ls -la .lighthouseci/ || echo "Directory is empty"
          else
            echo "Lighthouse reports directory does not exist"
          fi

      - name: Create GitHub Issue with Results
        if: always()
        uses: actions/github-script@v6
        with:
          github-token: ${{ github.token }}
          script: |
            const fs = require('fs');
            const path = require('path');

            const lhciDir = '.lighthouseci';

            // .lighthouseci 디렉토리 확인
            if (!fs.existsSync(lhciDir)) {
              console.log('No lighthouse reports found');
              return;
            }

            const jsonReports = fs.readdirSync(lhciDir).filter(f => f.endsWith('.json'));

            if (jsonReports.length === 0) {
              console.log('No JSON reports found');
              return;
            }

            const latestReport = jsonReports.sort().reverse()[0];

            if (!latestReport) {
              console.log('Failed to get latest report');
              return;
            }

            const reportPath = path.join(lhciDir, latestReport);

            if (!fs.existsSync(reportPath)) {
              console.log(`Report file not found: ${reportPath}`);
              return;
            }

            let rawReport;
            try {
              const reportContent = fs.readFileSync(reportPath, 'utf8');
              rawReport = JSON.parse(reportContent);
              console.log(`Report file loaded: ${latestReport}`);
              console.log(`Report type: ${Array.isArray(rawReport) ? 'Array' : typeof rawReport}`);
              
              if (Array.isArray(rawReport)) {
                console.log(`Report array length: ${rawReport.length}`);
                if (rawReport.length > 0) {
                  console.log(`First item keys: ${Object.keys(rawReport[0]).join(', ')}`);
                }
              } else {
                console.log(`Report keys: ${Object.keys(rawReport).join(', ')}`);
              }
            } catch (e) {
              console.log(`Failed to parse report file: ${e.message}`);
              return;
            }

            // Lighthouse 리포트는 배열, lhr 객체 안에 있거나 직접 최상위에 있을 수 있음
            let report;
            if (Array.isArray(rawReport)) {
              report = rawReport[0]; // 배열인 경우 첫 번째 리포트 사용
            } else {
              report = rawReport.lhr || rawReport;
            }

            // 디버깅 정보
            console.log('Final report type:', typeof report);
            if (report && typeof report === 'object') {
              if (report.categories) {
                console.log('Categories found:', Object.keys(report.categories));
              } else {
                console.log('No categories found in report');
              }
              if (report.audits) {
                console.log('Audits found, sample keys:', Object.keys(report.audits).slice(0, 5));
              } else {
                console.log('No audits found in report');
              }
            } else {
              console.log('Invalid report structure');
            }

            const formatScore = (value, withPercent = true) => {
              if (value === null || value === undefined) return 'N/A';
              const score = Math.round(value * 100);
              return withPercent ? `${score}%` : score;
            };

            const getEmoji = (value, metric) => {
              if (value === null || value === undefined || value === 'N/A') return '⚪';
              
              const thresholds = {
                LCP: { good: 2500, needsImprovement: 4000 },
                INP: { good: 200, needsImprovement: 500 },
                CLS: { good: 0.1, needsImprovement: 0.25 },
              };
              
              if (!thresholds[metric]) {
                const numValue = typeof value === 'number' ? value : parseFloat(value);
                if (isNaN(numValue)) return '⚪';
                return numValue >= 90 ? '🟢' : numValue >= 50 ? '🟠' : '🔴';
              }
              
              const t = thresholds[metric];
              const numValue = typeof value === 'number' ? value : parseFloat(value);
              if (isNaN(numValue)) return '⚪';
              
              if (metric === 'CLS') {
                return numValue <= t.good ? '🟢' : numValue <= t.needsImprovement ? '🟠' : '🔴';
              } else {
                return numValue <= t.good ? '🟢' : numValue <= t.needsImprovement ? '🟠' : '🔴';
              }
            };

            const formatMetric = (value, metric) => {
              if (value === null || value === undefined) return 'N/A';
              if (metric === 'CLS') return value.toFixed(3);
              return `${(value / 1000).toFixed(2)}s`;
            };

            const getLighthouseResult = (report, category) => {
              try {
                const categoryData = report.categories?.[category];
                if (!categoryData) return null;
                return categoryData.score ?? null;
              } catch (e) {
                console.log(`Error getting ${category}: ${e.message}`);
                return null;
              }
            };

            const getMetricValue = (report, metric) => {
              try {
                const audit = report.audits?.[metric];
                if (!audit) return null;
                return audit.numericValue ?? null;
              } catch (e) {
                console.log(`Error getting ${metric}: ${e.message}`);
                return null;
              }
            };

            const lighthouseScores = {
              performance: getLighthouseResult(report, 'performance'),
              accessibility: getLighthouseResult(report, 'accessibility'),
              'best-practices': getLighthouseResult(report, 'best-practices'),
              seo: getLighthouseResult(report, 'seo'),
              pwa: getLighthouseResult(report, 'pwa')
            };

            // 디버깅: 점수 확인
            console.log('Lighthouse scores:', lighthouseScores);

            const webVitals = {
              LCP: getMetricValue(report, 'largest-contentful-paint'),
              INP: getMetricValue(report, 'experimental-interaction-to-next-paint'),
              CLS: getMetricValue(report, 'cumulative-layout-shift')
            };

            // 디버깅: Web Vitals 확인
            console.log('Web Vitals:', webVitals);

            const reportUrl = `.lighthouseci/${latestReport.replace('.json', '.html')}`;

            // 커밋 정보 가져오기
            const commitSha = context.sha;
            const commitMessage = context.payload.head_commit?.message || 'No commit message';
            const commitLink = `https://github.com/${context.repo.owner}/${context.repo.repo}/commit/${commitSha}`;
            const shortSha = commitSha ? commitSha.substring(0, 7) : 'unknown';

            const body = `## 🚨 웹사이트 성능 측정 결과

            ### 📌 최신 커밋 정보
            - **커밋**: [\`${shortSha}\`](${commitLink})
            - **커밋 메시지**: ${commitMessage.split('\n')[0].trim()}

            ### 🎯 Lighthouse 점수
            | 카테고리 | 점수 | 상태 |
            |----------|------|------|
            | Performance | ${formatScore(lighthouseScores.performance)} | ${getEmoji(formatScore(lighthouseScores.performance, false))} |
            | Accessibility | ${formatScore(lighthouseScores.accessibility)} | ${getEmoji(formatScore(lighthouseScores.accessibility, false))} |
            | Best Practices | ${formatScore(lighthouseScores['best-practices'])} | ${getEmoji(formatScore(lighthouseScores['best-practices'], false))} |
            | SEO | ${formatScore(lighthouseScores.seo)} | ${getEmoji(formatScore(lighthouseScores.seo, false))} |
            | PWA | ${formatScore(lighthouseScores.pwa)} | ${getEmoji(formatScore(lighthouseScores.pwa, false))} |

            ### 📊 Core Web Vitals (2024)
            | 메트릭 | 설명 | 측정값 | 상태 |
            |--------|------|--------|------|
            | LCP | Largest Contentful Paint | ${formatMetric(webVitals.LCP, 'LCP')} | ${getEmoji(webVitals.LCP, 'LCP')} |
            | INP | Interaction to Next Paint | ${formatMetric(webVitals.INP, 'INP')} | ${getEmoji(webVitals.INP, 'INP')} |
            | CLS | Cumulative Layout Shift | ${formatMetric(webVitals.CLS, 'CLS')} | ${getEmoji(webVitals.CLS, 'CLS')} |

            ### 📝 Core Web Vitals 기준값
            - **LCP (Largest Contentful Paint)**: 가장 큰 콘텐츠가 화면에 그려지는 시점 
              - 🟢 Good: < 2.5s
              - 🟠 Needs Improvement: < 4.0s
              - 🔴 Poor: ≥ 4.0s

            - **INP (Interaction to Next Paint)**: 사용자 상호작용에 대한 전반적인 응답성
              - 🟢 Good: < 200ms
              - 🟠 Needs Improvement: < 500ms
              - 🔴 Poor: ≥ 500ms

            - **CLS (Cumulative Layout Shift)**: 페이지 로드 중 예기치 않은 레이아웃 변경의 정도
              - 🟢 Good: < 0.1
              - 🟠 Needs Improvement: < 0.25
              - 🔴 Poor: ≥ 0.25

            > 📅 측정 시간: ${new Date().toLocaleString('ko-KR', { timeZone: 'Asia/Seoul' })}`;

            try {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: `📊 웹사이트 성능 측정 결과 - ${new Date().toLocaleString('ko-KR', { 
                  timeZone: 'Asia/Seoul',
                  year: 'numeric',
                  month: '2-digit',
                  day: '2-digit',
                  hour: '2-digit',
                  minute: '2-digit',
                  hour12: false
                })}`,
                body: body,
                labels: ['lighthouse-audit', 'web-vitals']
              });
              console.log('Issue created successfully');
            } catch (error) {
              console.log(`Failed to create issue: ${error.message}`);
              // 레이블이 없으면 레이블 없이 재시도
              if (error.status === 422) {
                try {
                  await github.rest.issues.create({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    title: `📊 웹사이트 성능 측정 결과 - ${new Date().toLocaleString('ko-KR', { 
                      timeZone: 'Asia/Seoul',
                      year: 'numeric',
                      month: '2-digit',
                      day: '2-digit',
                      hour: '2-digit',
                      minute: '2-digit',
                      hour12: false
                    })}`,
                    body: body
                  });
                  console.log('Issue created without labels');
                } catch (retryError) {
                  console.log(`Failed to create issue without labels: ${retryError.message}`);
                }
              }
            }
